"""SQLAlchemy ORM model for a user's clothing item."""

# TODO: define ClothingItem model — id, user_id (FK), image_path, status (pending/ready/failed),
#       clothing_type, color, style, season, ai_description, created_at, updated_at
