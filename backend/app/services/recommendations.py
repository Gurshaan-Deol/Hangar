"""Outfit recommendation service — matches wardrobe items to current weather conditions."""

# TODO: implement get_recommendations(user_id, db, weather) -> list[OutfitRecommendation]
#       1. Load all ready ClothingItems for the user
#       2. Call AI provider generate_outfit_recommendation(items, weather)
#       3. Return ranked outfit suggestions
